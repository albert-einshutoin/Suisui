# Copy this file to packaging/signing.env for a local release machine,
# or export the same variables in a CI job. Keep certificate material in
# Keychain or the CI provider secret store.

SUISUI_SIGNING_IDENTITY="Developer ID Application: Example Name (TEAMID)"
SUISUI_CODESIGN_KEYCHAIN=""
SUISUI_REQUIRE_SIGNING=1
