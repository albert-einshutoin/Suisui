# Copy this file to packaging/notarization.env for a local release machine,
# or export the same variables in a CI job. Keep Apple credentials in Keychain
# through `xcrun notarytool store-credentials`.

SUISUI_NOTARY_PROFILE=SuisuiNotaryProfile
SUISUI_REQUIRE_NOTARIZATION=1
