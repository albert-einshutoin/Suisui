[package]
name = "suisui-kokoro-helper"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
ort = { version = "=2.0.0-rc.11", default-features = false, features = ["copy-dylibs", "download-binaries", "std", "tls-rustls"] }
